server:
  port: 8081

spring:
  application:
    name: course-service

  datasource:
    url: jdbc:mysql://localhost:3306/lms
    username: root
    password: test

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
